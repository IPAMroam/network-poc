---
  vars:
  cli:
    hosts: hosts
    username: admin_mlu6
    password: cbcdj123!
    transport: cli
    
- name: Test NXOS
  nxos_command:
    commands: 
          - show version
          - show interfaces 
    provider: "{{ cli }}"
    
