---
- name: Test NXOS
  hosts: hosts
  
  vars:
  cli:
    username: admin_mlu6
    password: cbcdj123!
    transport: cli

  nxos:
    commands: 
          - show version
          - show interfaces 
    provider: "{{ cli }}"
    
